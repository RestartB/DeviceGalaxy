services:
  devicegalaxy:
    image: restartb/devicegalaxy:latest
    container_name: devicegalaxy
    restart: unless-stopped
    ports:
      - '3000:3000'
    volumes:
      - './data:/data'
      - './db:/db'
    env_file:
      - .env
